/// App-wide constant values.
enum Constants {
  /// Constants used by the Sentry distribution / auto-update flow.
  enum Distribution {
    static let accessToken = "SENTRY_DISTRIBUTION_AUTH_TOKEN"
  }
}
